// Intervalos e Timeout
function escrever() {
    console.log('Geek University');
}

// Intervalo - Fica executando em looping
setInterval(escrever, 5000); //5 segundos

//Timeout (tempo esgotado) //delay - atrasos
setTimeout(escrever, 3000); //3 segundos